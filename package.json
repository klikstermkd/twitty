{
  "name": "twitty",
  "version": "0.1.0",
  "devDependencies": {
    "cpy-cli": "^1.0.1",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && cpy now.package.json ./build --rename=package.json && now ./build"
  }
}
